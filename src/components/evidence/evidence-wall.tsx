"use client";import{useState}from"react";import{EVIDENCE,type EvidenceCategory}from"@/content/portfolio/evidence";import{EvidenceCard}from"./evidence-card";import{ProofNavigator}from"@/components/portfolio/proof-navigator";export function EvidenceWall(){const[filter,setFilter]=useState<EvidenceCategory|null>(null);const items=filter?EVIDENCE.filter(e=>e.category===filter):EVIDENCE;return <><ProofNavigator onChange={setFilter}/><div className="evidence-grid">{items.map(e=><EvidenceCard key={e.id} item={e}/>)}</div></>}
