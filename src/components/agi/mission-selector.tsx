"use client";import{useState}from"react";import{PUBLIC_MISSIONS}from"@/content/agi/public-missions";import{SpecialistFlow}from"./specialist-flow";import{trackPublicEvent}from"@/lib/analytics/events";export function MissionSelector(){const[selected,setSelected]=useState<(typeof PUBLIC_MISSIONS)[number]["id"]>(PUBLIC_MISSIONS[0].id);const m=PUBLIC_MISSIONS.find(x=>x.id===selected)??PUBLIC_MISSIONS[0];return <div className="agi-mission-layout"><div className="mission-buttons" role="group" aria-label="Misión de ejemplo">{PUBLIC_MISSIONS.map(x=><button type="button" key={x.id} className="mission-button" aria-pressed={selected===x.id} onClick={()=>{setSelected(x.id);trackPublicEvent("agi_mission_selected",{mission:x.id,funnel:"corporate"})}}>{x.label}</button>)}</div><div><p className="h-eyebrow">VISUAL_ONLY · DEMOSTRACIÓN</p><h3 className="h-h3">{m.label}</h3><p className="h-copy">{m.summary}</p><SpecialistFlow specialists={m.specialists}/><p className="agi-note">Esta experiencia no invoca runtimes productivos, memoria privada ni herramientas internas. Las acciones materiales permanecen gobernadas por políticas, evidencia y Owner Gate cuando corresponde.</p></div></div>}
