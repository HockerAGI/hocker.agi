import{PROFILE}from"@/content/portfolio/profile";import{ContactActions}from"@/components/ui/contact-actions";export function PortfolioContact(){const links=Object.entries(PROFILE.socials).filter((x):x is[string,string]=>Boolean(x[1]));return <div className="founder-bridge"><div><p className="h-eyebrow">CONTACTO PROFESIONAL</p><h2 className="h-h2">¿Crees que puedo aportar a tu equipo?</h2></div><aside><a className="button button-primary" href={`mailto:${PROFILE.email}`}>Hablemos</a><ContactActions compact/>{links.map(([n,u])=><a className="text-link" href={u} key={n} target="_blank" rel="noopener noreferrer">{n}<span>↗</span></a>)}</aside></div>}
