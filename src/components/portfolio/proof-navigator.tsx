"use client";import{useState}from"react";import type{EvidenceCategory}from"@/content/portfolio/evidence";const dims:[[EvidenceCategory,string],...[EvidenceCategory,string][]]=[["ai","IA & Automatización"],["engineering","Producto & Desarrollo"],["growth","Growth & Marketing"],["creative","Creative & Brand"],["leadership","Liderazgo & Operación"]];export function ProofNavigator({onChange}:{onChange:(c:EvidenceCategory|null)=>void}){const[selected,setSelected]=useState<EvidenceCategory|null>(null);return <div className="proof-nav" role="group" aria-label="Filtrar evidencia"><button type="button" aria-pressed={selected===null} onClick={()=>{setSelected(null);onChange(null)}}>Todo</button>{dims.map(([id,l])=><button type="button" key={id} aria-pressed={selected===id} onClick={()=>{setSelected(id);onChange(id)}}>{l}</button>)}</div>}
