import type{Metadata}from"next";import Image from"next/image";import{notFound}from"next/navigation";import{APPS}from"@/lib/site-data";import{publicReadiness}from"@/lib/product-readiness";import{TextLink}from"@/components/ui/text-link";export function generateStaticParams(){return APPS.map(a=>({slug:a.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=APPS.find(x=>x.slug===slug);return a?{title:a.title,description:a.summary,alternates:{canonical:`/apps/${a.slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=APPS.find(x=>x.slug===slug);if(!a)notFound();const r=publicReadiness(a.slug,a.availability);return <><section className="page-hero"><div className="container portfolio-hero-grid"><div><p className="h-eyebrow">{a.badge} · {r.state}</p><h1 className="h-h2">{a.title}</h1><p className="h-lead">{a.tagline}</p><p className="h-copy">{r.label}</p></div><Image src={a.logo} alt={`Logo oficial de ${a.title}`} width={1600} height={600} className="app-detail-logo"/></div></section><section className="section"><div className="container"><div className="route-grid"><article className="route-card"><p className="h-eyebrow">PROPÓSITO</p><h2 className="h-h3">{a.short}</h2><p>{a.summary}</p></article><article className="route-card"><p className="h-eyebrow">PARA QUIÉN</p><h2 className="h-h3">Contexto de uso</h2><p>{a.audience}</p><p>{a.highlights.join(" · ")}</p></article></div><div className="actions"><TextLink href="/apps">Volver a productos</TextLink><TextLink href="/contacto">Evaluar un caso</TextLink></div></div></section></>}
