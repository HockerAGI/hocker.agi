import type{Metadata}from"next";import Link from"next/link";import{PORTFOLIO_CASES}from"@/content/portfolio/cases";export const metadata:Metadata={title:"Casos y evidencia",description:"Capacidades y trabajo explicados por problema, contribución y evidencia.",alternates:{canonical:"/casos"}};export default function Page(){return <><section className="page-hero"><div className="container"><p className="h-eyebrow">CASOS</p><h1 className="h-h2">La prueba importa más que la promesa.</h1><p className="h-lead">Los casos públicos distinguen entre evidencia disponible, trabajo documentado y material que todavía no puede mostrarse.</p></div></section><section className="section"><div className="container selected-work">{PORTFOLIO_CASES.map(c=><Link href={`/portafolio/${c.slug}`} className="work-row" key={c.slug}><span className="status">{c.status}</span><div><h3>{c.title}</h3><strong>{c.strapline}</strong></div><p>{c.summary}</p></Link>)}</div></section></>}
