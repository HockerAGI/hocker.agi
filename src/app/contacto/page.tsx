import type{Metadata}from"next";import{LeadForm}from"@/components/lead-form";import{PUBLIC_SITE}from"@/lib/public-site";export const metadata:Metadata={title:"Contacto",description:"Cuéntanos qué quieres crear y prepara el siguiente paso.",alternates:{canonical:"/contacto"}};export default function Page(){return <section className="section"><div className="container contact-layout"><div><p className="h-eyebrow">CONTACTO</p><h1 className="h-h2">¿Qué quieres crear?</h1><p className="h-lead">Objetivo, contexto y restricciones. Con eso podemos definir el siguiente paso sin vender tecnología que no necesitas.</p><p className="h-copy"><a href={`mailto:${PUBLIC_SITE.email}`}>{PUBLIC_SITE.email}</a><br/>{PUBLIC_SITE.phoneDisplay}</p></div><LeadForm/></div></section>}
