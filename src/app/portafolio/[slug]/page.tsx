import type{Metadata}from"next";import{notFound}from"next/navigation";import{PORTFOLIO_CASES}from"@/content/portfolio/cases";import{EVIDENCE}from"@/content/portfolio/evidence";import{EvidenceCard}from"@/components/evidence/evidence-card";export function generateStaticParams(){return PORTFOLIO_CASES.map(c=>({slug:c.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=PORTFOLIO_CASES.find(x=>x.slug===slug);return c?{title:`${c.title} · Caso profesional`,description:c.summary,alternates:{canonical:`/portafolio/${c.slug}`}}:{}}export default async function PortfolioCasePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=PORTFOLIO_CASES.find(x=>x.slug===slug);if(!c)notFound();const proof=EVIDENCE.filter(e=>c.proofIds.includes(e.id));return <><section className="page-hero"><div className="container"><p className="h-eyebrow">CASO · {c.status}</p><h1 className="h-h2">{c.title}</h1><p className="h-lead">{c.strapline}</p><p className="h-copy">{c.role}</p></div></section><section className="section"><div className="container"><div className="section-head"><div><p className="h-eyebrow">RETO</p><h2 className="h-h3">{c.challenge}</h2></div><div><p className="h-eyebrow">MI CONTRIBUCIÓN</p><ul className="h-copy">{c.contribution.map(x=><li key={x}>{x}</li>)}</ul></div></div><div className="evidence-grid">{proof.map(e=><EvidenceCard key={e.id} item={e}/>)}</div></div></section></>}
