import type{MetadataRoute}from"next";import{APPS}from"@/lib/site-data";import{PORTFOLIO_CASES}from"@/content/portfolio/cases";import{PUBLIC_SITE}from"@/lib/public-site";const routes=["/","/soluciones","/apps","/casos","/portafolio","/empresa","/ecosistema","/agis","/contacto","/legal/privacy","/legal/terms"]as const;export default function sitemap():MetadataRoute.Sitemap{return[...routes.map(p=>({url:`${PUBLIC_SITE.url}${p}`})),...APPS.map(a=>({url:`${PUBLIC_SITE.url}/apps/${a.slug}`})),...PORTFOLIO_CASES.map(c=>({url:`${PUBLIC_SITE.url}/portafolio/${c.slug}`}))]}
