import test from "node:test";import assert from "node:assert/strict";import{existsSync,readFileSync,readdirSync}from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");function walk(directory){return readdirSync(directory,{withFileTypes:true}).flatMap(entry=>{const absolute=path.join(directory,entry.name);return entry.isDirectory()?walk(absolute):[absolute]})}function resolveAlias(specifier){const relative=specifier.slice(2),base=path.join(root,"src",relative),candidates=[base,`${base}.ts`,`${base}.tsx`,path.join(base,"index.ts"),path.join(base,"index.tsx")];return candidates.find(candidate=>existsSync(candidate))??null}
test("every @/ source import resolves locally",()=>{const sourceFiles=walk(path.join(root,"src")).filter(file=>/\.(ts|tsx)$/.test(file));for(const file of sourceFiles){const source=readFileSync(file,"utf8");for(const match of source.matchAll(/from\s*["'](@\/[^"']+)["']/g))assert.ok(resolveAlias(match[1]),`${path.relative(root,file)} -> ${match[1]} must resolve`)}});
test("approved public routes have concrete page files",()=>{for(const route of["src/app/page.tsx","src/app/soluciones/page.tsx","src/app/apps/page.tsx","src/app/apps/[slug]/page.tsx","src/app/agis/page.tsx","src/app/casos/page.tsx","src/app/ecosistema/page.tsx","src/app/empresa/page.tsx","src/app/contacto/page.tsx","src/app/portafolio/page.tsx","src/app/portafolio/[slug]/page.tsx","src/app/legal/privacy/page.tsx","src/app/legal/terms/page.tsx"])assert.ok(existsSync(path.join(root,route)),route)});
